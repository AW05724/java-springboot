<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.repository.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.repository.mapper.User">
        <id property="id" column="id"/>
        <result property="mail" column="mail"/>
        <result property="password" column="password"/>
        <result property="roles" column="roles"/>
        <result property="created" column="created"/>
        <result property="lastLogined" column="last_logined"/>
        <result property="enabled" column="enabled"/>
    </resultMap>

    <select id="findAlls" resultMap="UserResultMap">
        SELECT * FROM `user`
    </select>

    <select id="findByIds" parameterType="Integer" resultMap="UserResultMap">
        SELECT * FROM `user` WHERE id = #{id}
    </select>

</mapper>
